FROM python:3.12-slim

WORKDIR /app
COPY . .
RUN pip install --no-cache-dir '.[catalog]'

EXPOSE 8000
VOLUME /data

CMD ["uvicorn", "anyzork.catalog_api:create_catalog_app", "--factory", "--host", "0.0.0.0", "--port", "8000"]
